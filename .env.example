# Public values are inlined at build time, so changing them requires a rebuild.
NEXT_PUBLIC_APP_NAME="Next.js Deploy Template"

# Used by next/node for environment-specific behaviour.
NODE_ENV=development
